
# --- Demo & tuning ---
# DEMO_MODE=true  → any email domain registers; verification code shown on screen
# DEMO_MODE=true
# Minimum blended score for search results (default 0.45)
# SEARCH_MIN_SCORE=0.45
# Public base for locally-stored photos when Spaces is unset and the API sits
# behind a path prefix (prod: https://<your-app>.ondigitalocean.app/api)
# UPLOADS_PUBLIC_BASE=
